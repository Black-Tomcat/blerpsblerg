import React from "react";

export interface EditorCommandsProps {
}

const EditorCommands: React.FC<EditorCommandsProps> = (props: EditorCommandsProps) => {
    return null;
};

export default EditorCommands;
